{
  "name": "todo-list-netlify",
  "version": "1.0.0",
  "description": "Todo List com salvamento em JSON via Netlify Functions",
  "main": "index.html",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'No build step needed for static site'"
  },
  "dependencies": {},
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}

